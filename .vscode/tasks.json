{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and run",
            "type": "shell",
            "command": "zig build run",
            "group": "build"
        }
    ]
}
